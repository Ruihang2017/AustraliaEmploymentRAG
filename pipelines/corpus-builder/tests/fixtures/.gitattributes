# CRPS-08, mirroring the CRPS-01/CRPS-02/CRPS-03 precedent: keep LF in the working tree, because
# git core.autocrlf=true on a Windows checkout would otherwise rewrite these files and the suite
# compares generated bytes exactly.
* text eol=lf
