# INGF-02, mirroring the INGF-01 / CRPS-01 / CRPS-03 precedent. Keep LF in the working tree (git
# core.autocrlf=true would otherwise rewrite these on a Windows checkout, and the fetch suite
# asserts byte-level behaviour).
* text eol=lf
