{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:taxrag:events:webhook:v1:alert.created",
  "title": "alert.created webhook event",
  "description": "The only webhook event type PRD §34.8 fully specifies (docs/PRD.md lines 2099-2112). Self-contained on purpose — see the `data` note in envelope.json. Payload minimisation is structural, not a review comment: PRD §34.8 excludes full questions, facts, answers and source excerpts by default, and §8.8/§22 say the same. This event carries identifiers and structured metadata only.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "type",
    "created_at",
    "sandbox",
    "data"
  ],
  "properties": {
    "schema_version": {
      "description": "PRD §16.1: webhooks carry their own schema version. Today it is exactly \"1.0\"; a non-additive change mints schemas/events/webhook/v2/** and bumps this const.",
      "type": "string",
      "const": "1.0"
    },
    "id": {
      "description": "Opaque event id, `evt_`-prefixed UUIDv7 (PRD §34.1, §34.8). Receivers deduplicate on it. The pattern is the canonical lower-case RFC 9562 v7 form that packages/contracts/src/ids/uuidv7.ts mints.",
      "type": "string",
      "pattern": "^evt_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "type": {
      "description": "Narrowed to this file's event type.",
      "type": "string",
      "const": "alert.created"
    },
    "created_at": {
      "description": "RFC 3339 UTC instant, seconds precision, always suffixed `Z` (PRD §34.8 line 2103).",
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "sandbox": {
      "description": "Whether the delivery originates from the sandbox environment (PRD §34.8 line 2104).",
      "type": "boolean"
    },
    "data": {
      "description": "PRD §34.8 lines 2105-2111, in the PRD's own key order.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "alert_id",
        "watchlist_id",
        "change_type",
        "effective_date",
        "affected_research_record_ids"
      ],
      "properties": {
        "alert_id": {
          "description": "Opaque `alt_`-prefixed UUIDv7 (PRD §34.8 line 2106).",
          "type": "string",
          "pattern": "^alt_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
        },
        "watchlist_id": {
          "description": "Opaque `wat_`-prefixed UUIDv7 (PRD §34.8 line 2107).",
          "type": "string",
          "pattern": "^wat_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
        },
        "change_type": {
          "description": "PRD §8.8: changes MUST be structured events, not raw HTML diffs. The members are FND-03's CHANGE_TYPE_VALUES (packages/contracts/src/enums/change-type.ts); schemas.test.ts asserts this enum equals that constant, so the two cannot drift.",
          "type": "string",
          "enum": [
            "AMENDMENT",
            "COMMENCEMENT",
            "RATE",
            "REPLACEMENT",
            "APPEAL",
            "GUIDANCE",
            "SOURCE_REMOVAL",
            "FRESHNESS"
          ]
        },
        "effective_date": {
          "description": "Calendar date the change takes effect, ISO 8601 (PRD §34.8 line 2109).",
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
        },
        "affected_research_record_ids": {
          "description": "Opaque `rec_`-prefixed UUIDv7 identifiers only — never record content (PRD §34.8 line 2110, §22). May be empty when the change affects no stored record.",
          "type": "array",
          "minItems": 0,
          "items": {
            "type": "string",
            "pattern": "^rec_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
          }
        }
      }
    }
  }
}
