{
  "$comment": [
    "PRD section 20.2: coding agents MUST NOT receive production credentials by default, so no PRD",
    "section 45.3 entry command may read one. The scan covers root package.json scripts and tools/**.",
    "",
    "This file is the ONLY excluded path, matched by exact relative path — never by a fixtures/ wildcard,",
    "which would also blind the scan to every future fixture. The scanner has a positive control in",
    "tools/tests/secret-scan.test.mjs: it is fed a synthetic credential name built at runtime from parts",
    "(so the literal never appears in a scanned file) and must detect it. A scanner that cannot detect",
    "anything discharges nothing."
  ],
  "excludedPaths": ["tools/fixtures/secret-patterns.json"],
  "scanRoots": ["tools"],
  "scanRootFiles": ["package.json"],
  "patterns": [
    { "id": "token", "regex": "\\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*_TOKEN\\b", "why": "generic bearer/API token env var" },
    { "id": "secret", "regex": "\\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*_SECRET(?:_[A-Z0-9]+)*\\b", "why": "generic secret env var" },
    { "id": "key", "regex": "\\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*_KEY(?:_[A-Z0-9]+)*\\b", "why": "generic API/private key env var" },
    { "id": "aws", "regex": "\\bAWS_[A-Z0-9_]+\\b", "why": "PRD section 19 AWS S3 Sydney credentials" },
    { "id": "password", "regex": "\\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*_PASSWORD\\b", "why": "database or service password" },
    { "id": "credential", "regex": "\\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*_CREDENTIALS?\\b", "why": "provider credential bundle" },
    { "id": "private-key-block", "regex": "-----BEGIN [A-Z ]*PRIVATE KEY-----", "why": "an inlined signing or SSH key" },
    { "id": "dsn", "regex": "\\b(?:DATABASE|POSTGRES|REDIS|SENTRY)_(?:URL|DSN)\\b", "why": "connection string carrying credentials" }
  ],
  "positiveControlParts": ["AWS", "SECRET", "ACCESS", "KEY"]
}
